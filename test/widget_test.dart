import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — camera hardware is unavailable in test environments.
  // Integration tests for the camera pipeline live in integration_test/.
  test('placeholder', () => expect(true, isTrue));
}
